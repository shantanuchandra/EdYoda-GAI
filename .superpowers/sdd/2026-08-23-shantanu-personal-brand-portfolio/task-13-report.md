# Task 13 report — Vercel preview deployment

Date: 23 August 2026 (Asia/Kolkata)

## Outcome

- Ready protected preview: `https://shantanu-chandra-portfolio-33i6ag7p0-shantanuchandras-projects.vercel.app`
- Deployment ID: `dpl_9vudzS66zUxAkERTUrYCP5WEHK6W`
- Target/status: `preview` / `READY`
- Project: `shantanuchandras-projects/shantanu-chandra-portfolio`
- Project ID: `prj_Jg0p5A7goJaWEZi0Qcp0rjD1aKGY`
- Account/scope: `shantanuchandra` / `shantanuchandras-projects`
- Local Vercel CLI: upgraded from `58.9.4` to exact `59.5.0` with `npm i -g vercel@latest`

The linked project is the new isolated `shantanu-chandra-portfolio` project. At every deploy boundary, `.vercel/project.json` was checked for that exact name and would have aborted on `ed-yoda-gai`. The pre-existing `ed-yoda-gai` project was never linked, inspected mutatively, modified, or deployed.

## Preflight and project boundary

Read-only preflight at required base `19e5aa2e891cc37788025355e434bb52afcdbe55` showed a clean worktree, no `portfolio/.vercel/project.json`, authenticated user `shantanuchandra`, scope `shantanuchandras-projects`, and no pre-existing project named `shantanu-chandra-portfolio`. The account project list contained an unrelated `ed-yoda-gai` project; it was not selected.

Commands included:

```text
git rev-parse HEAD
git status --short --branch
vercel --version
vercel whoami
vercel teams ls
vercel project ls
vercel project inspect shantanu-chandra-portfolio
npm i -g vercel@latest
vercel project add shantanu-chandra-portfolio --scope shantanuchandras-projects
vercel link --yes --team shantanuchandras-projects --project shantanu-chandra-portfolio
vercel project inspect shantanu-chandra-portfolio --scope shantanuchandras-projects
```

The link created `portfolio/.vercel/project.json` with project name `shantanu-chandra-portfolio`. `portfolio/.gitignore` now ignores `.vercel/`; `git check-ignore` proves both the directory and `project.json` are ignored, and `git ls-files .vercel` is empty. The ignored `.env.local` generated by `vercel link` was removed without displaying its OIDC token.

Vercel project inspection reports Node.js `24.x`. It reports Root Directory `.` because the CLI project itself is linked at the `portfolio/` application boundary; deployment metadata records `gitRootDirectory: portfolio`. Thus the effective repository Root Directory is `portfolio`, and no repository-root content was uploaded. The project setting remains Framework `Other`, while the versioned `vercel.ts` applies `framework: "nextjs"` and `buildCommand: "pnpm build"`; the successful remote build detected Next.js 16.3.2 and logged `Applying modifyConfig from Vercel`. Fluid Compute has no project or function override and therefore uses Vercel's default for this new project.

## Configuration and documentation

- Added `portfolio/vercel.ts` with a typed `VercelConfig` export from `@vercel/config/v1`, framework `nextjs`, and build command `pnpm build`.
- Added `portfolio/README.md` with the exact project name, repository Root Directory `portfolio`, Node.js 24.x, Fluid Compute defaults, `content/manifest.ts` public allowlist boundary, and release flow `pnpm verify → preview deployment → human review → separate production approval`.
- No Edge runtime, rewrite, integration, secret, legacy `vercel.json`, tracking, domain, or custom alias was added.
- `@vercel/config` was already an exact direct dependency, so it was not changed.

## Pre-deploy verification and deployment fixes

The default port 3000 was occupied by an unrelated long-running `Naina2` Next.js server, which was not stopped. Verification therefore used the repository-supported isolated port override:

```text
PLAYWRIGHT_PORT=3013 pnpm verify
```

Fresh final result after all deployment fixes:

- lint: PASS
- TypeScript: PASS
- unit tests: 72/72 PASS across 13 files
- content validator: exactly 10 allowlisted files / 10 public
- public-copy validator: 73 text files and 1 PDF PASS
- Next.js production build: PASS, 24 static/SSG routes generated
- Playwright: 114/114 PASS across Chromium, Firefox, and WebKit
- responsive E2E: all 18 sitemap routes at 320, 390, 768, 1024, and 1440 widths
- accessibility: 18 routes, zero serious/critical axe violations in all three browsers

Two deployment-only portability problems were fixed without weakening gates:

1. pnpm 11 removed the existing `onlyBuiltDependencies` setting. A clean isolated pnpm 11.0.5 install reproduced `ERR_PNPM_IGNORED_BUILDS`. `pnpm-workspace.yaml` now uses the pnpm 11 `allowBuilds` map for exact `esbuild@0.28.2` and `unrs-resolver@1.12.2` only. A clean frozen install then ran exactly those two reviewed scripts and `pnpm ignored-builds` reported `None`.
2. Vercel's build image lacks `pdftotext`. A RED regression generated a valid PDF containing forbidden `EdYoda` text and proved the validator failed when the executable was absent. The validator now uses exact `unpdf@1.8.1`, a maintained Node >=22/serverless PDF.js parser, and the GREEN regression proves forbidden PDF text is still detected with no system executable. Parser failures remain fatal; PDF validation is not skipped or environment-conditioned.

## Deployment history and safety anomaly

All intended preview commands after discovery used explicit `--target=preview`.

| Deployment | Target | Status | Evidence |
|---|---|---|---|
| `dpl_7vTZwaGhDRyCK2waEdFo8FiwCxmJ` | production | ERROR | First command was `vercel deploy --yes --scope ...` with no `--prod`; unexpectedly classified by Vercel as the first production target and failed during install before app build because pnpm 11 rejected the obsolete build policy. |
| `dpl_EY2ovz7m3FczRUCdthx7Sw25xvFW` | preview | ERROR | Explicit `--target=preview`; package install passed, then the build failed because `pdftotext` was absent. |
| `dpl_9vudzS66zUxAkERTUrYCP5WEHK6W` | preview | READY | Explicit `--target=preview`; dependency install, PDF/public copy gate, Next build, output deployment all passed. |

The first-deployment classification is contrary to the documented default-preview behavior and no exact cause was exposed by CLI/API inspection. The new project had no Git integration and no custom environments. Once discovered, no additional default-only deploy was attempted.

No `--prod`, promote, redeploy, alias, domain, rollback, integration, or deployment-protection mutation command was run. No pre-existing production deployment or domain belonging to any project was changed. However, it would be inaccurate to claim the new isolated project's production state was literally untouched: Vercel automatically created the failed production-target record above and assigned its generated project alias during the first no-`--prod` attempt. The application never built in that record; the only READY application deployment is the explicit Preview listed above. No custom domain was added or changed.

## Preview protection and live verification

The preview remains protected:

- direct `curl` returns HTTP 302 to Vercel SSO with `x-robots-tag: noindex`;
- authenticated `vercel curl` returns HTTP 200 without disabling protection;
- a short-lived CLI-generated bypass cookie was used only for protected Playwright verification and deleted immediately afterward without printing its value.

Because the preview is not publicly accessible, `pnpm check:links -- <preview URL>` would test the SSO redirect instead of the application. The protected-preview equivalent used authenticated browser/request coverage:

- all 18 sitemap routes returned HTTP 200 at both 390×844 and 1440×900;
- every route had exactly one `h1`, no horizontal overflow, required metadata, and no forbidden public text;
- JSON-LD parsed on the supported homepage, About, Work, Product, Learning, and Insight routes;
- keyboard skip-link focus/activation passed; mobile menu opened with Enter and closed with Escape;
- all four employer stories, both product stories, all three Learning Lab paths, the Insight, About, Contact, HTML Resume, PDF resume, sitemap, robots, and social image passed;
- 22 unique internal route/asset destinations returned 2xx/3xx through authenticated protected-preview requests;
- live PDF returned `application/pdf`, `%PDF`, parsed successfully with `unpdf`, and contained no forbidden text;
- OG image returned PNG at exactly 1200×630;
- Wasabi (`https://wasabitravels.com/`) was the only product external destination and returned HTTP 200;
- `cardcompass.in`, the removed brand, phone number, source-CV filename/path, unfinished markers, and dummy Latin copy were absent.

## Remaining concerns

1. The first no-`--prod` attempt created an ERROR production-target record and automatic generated alias in the new isolated project. It was not deleted because deletion was not authorized and the record is useful audit evidence.
2. Vercel project inspection displays `Root Directory .` and Framework `Other` relative to the CLI-linked `portfolio/` boundary. The effective uploaded Git root is `portfolio`, and `vercel.ts` successfully applies Next.js/build settings per deployment; a future Git integration should explicitly set dashboard Root Directory to `portfolio` and Framework to Next.js before any production approval.
3. The ready preview is protected, so stakeholder review requires Vercel access; protection was deliberately not weakened.
4. Existing non-fatal Vite native-config and peer-range warnings remain unchanged from prior tasks.

Production approval remains a separate human decision. No promotion or domain action was taken.
